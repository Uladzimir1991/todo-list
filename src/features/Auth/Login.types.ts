export type FormValuesType = {
    email: string,
    password: string,
    rememberMe: boolean,
};

export type FormikErrorType = {
    email?: string,
    password?: string,
    rememberMe?: boolean,
};